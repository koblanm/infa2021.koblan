#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int x;
    double a = 0;
    double b = 0;
    double c = 0;
    cin >> x;
    a = floor(x/100);
    b = - a*10 + floor(x/10);
    c = x - a*100 - b*10;
    cout << a+b+c << endl;
    return 0;
}
